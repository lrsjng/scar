{
  "name": "scar",
  "version": "1.1.0",
  "description": "Test runner for Node.js and the browser.",
  "homepage": "https://larsjung.de/scar/",
  "bugs": "https://github.com/lrsjng/scar/issues",
  "author": "Lars Jung <lrsjng@gmail.com> (https://larsjung.de)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lrsjng/scar.git"
  },
  "scripts": {
    "lint": "eslint .",
    "test:mocha": "mocha --compilers js:babel-core/register test/index-mocha",
    "test:scar": "node test",
    "test": "npm run -s test:mocha && npm run -s test:scar",
    "check": "npm run -s lint && npm run -s test",
    "cover": "isparta cover --dir coverage --report text --report html test",
    "build": "node ghu release",
    "precommit": "npm run -s check && npm run -s build"
  },
  "main": "dist/scar.min.js",
  "dependencies": {},
  "devDependencies": {
    "babel-core": "6.25.0",
    "babel-loader": "7.1.1",
    "babel-preset-es2015": "6.24.1",
    "eslint": "4.2.0",
    "ghu": "0.12.0",
    "isparta": "4.0.0",
    "mocha": "3.4.2"
  },
  "engines": {
    "node": ">=6.0.0"
  }
}
