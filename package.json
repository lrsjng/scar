{
  "name": "scar",
  "version": "0.6.0",
  "description": "A test runner for Node.js and the browser.",
  "homepage": "https://larsjung.de/scar/",
  "bugs": "https://github.com/lrsjng/scar/issues",
  "author": "Lars Jung <lrsjng@gmail.com> (https://larsjung.de)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lrsjng/scar.git"
  },
  "scripts": {
    "lint": "eslint .",
    "test:mocha": "mocha --compilers js:babel-core/register test/tests-mocha.js",
    "test:scar": "node -r babel-core/register test/tests-scar.js",
    "test": "npm run -s test:mocha && npm run -s test:scar",
    "cover:mocha": "babel-node node_modules/isparta/bin/isparta cover --dir coverage/mocha --report text --report html node_modules/mocha/bin/_mocha -- --reporter dot test/tests-mocha.js",
    "cover:scar": "babel-node node_modules/isparta/bin/isparta cover --dir coverage/scar --report text --report html test/tests-scar.js",
    "cover": "npm run -s cover:mocha && npm run -s cover:scar",
    "ghu": "node -r babel-core/register ghu.js",
    "build": "npm run -s ghu build",
    "prepublish": "npm run -s lint && npm run -s test && npm run -s build"
  },
  "main": "es5/scar.js",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "6.3.13",
    "babel-core": "6.3.13",
    "babel-eslint": "5.0.0-beta4",
    "babel-loader": "6.2.0",
    "babel-preset-es2015": "6.3.13",
    "eslint": "1.10.3",
    "ghu": "0.1.1",
    "isparta": "4.0.0",
    "mocha": "2.3.4"
  },
  "engines": {
    "node": ">=4.0.0"
  }
}
